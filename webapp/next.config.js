/** @type {import('next').NextConfig} */
const nextConfig = {
  // Server Actions are enabled by default in Next.js 16
  // Body size limit for API routes is now configured per-route
}

module.exports = nextConfig
